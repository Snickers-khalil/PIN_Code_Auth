name: android_test_task_master
description: A new Flutter project.

publish_to: 'none' 

# Remove this line if you wish to publish to pub.dev
# SAY HI TO MEE :) 
#SNICKERS

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  flutter_bloc: ^8.0.1
  hive: ^2.0.5
  hive_flutter: ^1.1.0


flutter:
  uses-material-design: true
